[pytest]
minversion = 7.0
testpaths = tests
pythonpath = src
addopts = -q --cov=risk_management.email_notifications --cov=risk_management._notifications --cov=risk_management.snapshot_utils --cov-report=term-missing --cov-fail-under=100
asyncio_mode = auto
markers =
    asyncio: mark test as requiring an asyncio event loop
